language: node_js
sudo: required
services:
  - docker

before_install:
  - docker build -t joshjacobbaker/docker-react -f Dockerfile.dev .

  script:
    - docker run -e CI=true joshjacobbaker/docker-react npm run test