language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t joshjacobbaker/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true joshjacobbaker/docker-react npm run test

deploy:
  provider: elasticbeanstalk
  region: "us-east-2"
  app: "docker-react"
  env: "DockerReact-env"
  bucket_name: "elasticbeanstalk-us-east-2-459542061631"
  bucket_path: "docker-react"
  on:
    branch: master
